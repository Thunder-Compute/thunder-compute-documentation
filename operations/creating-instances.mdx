---
title: "Creating Instances"
description: "Launch new Thunder Compute instances with your preferred configuration"
icon: "plus"
iconType: "solid"
---

Create a new Thunder Compute instance to start running GPU workloads. You can configure the mode, GPU type, CPU count, disk size, and template.

<Tabs>
  <Tab title="CLI">
    ### Interactive Mode

    Run the create command to launch an interactive menu:

    ```bash
    tnr create
    ```

    This walks you through selecting your instance configuration step by step.

    ### One-Line Creation

    Specify all options in a single command:

    ```bash
    tnr create --mode prototyping --gpu t4 --vcpus 8 --template base --disk-size-gb 100
    ```

    ### Configuration Options

    | Flag           | Description                                                                                                  |
    | -------------- | ------------------------------------------------------------------------------------------------------------ |
    | `--mode`       | `prototyping` or `production`                                                                                |
    | `--gpu`        | `t4` or `a100` (prototyping), `a100`, `a100xl`, or `h100` (production)                                       |
    | `--num-gpus`   | `1`, `2`, or `4` (production only)                                                                           |
    | `--vcpus`      | CPU cores: `4`, `8`, `16`, or `32` (prototyping only). RAM: 8GB per vCPU                                     |
    | `--template`   | `base`, `comfy-ui`, `comfy-ui-wan`, `ollama`, `webui-forge`, or a snapshot name                              |
    | `--disk-size-gb` | `100`-`400` GB (prototyping), `100`-`1000` GB (production)                                                 |

    ### Mode Selection

    Choose between optimized development pricing or full compatibility:

    - **Prototyping** (default): Lower cost with CUDA-level optimizations. Best for development.
    - **Production**: Standard VM with full compatibility. Best for long-running jobs and production workloads.

    See [Prototyping vs Production](/prototyping-vs-production) for details on each mode.

    ### GPU Options

    | GPU       | VRAM  | Availability         |
    |-----------|-------|----------------------|
    | T4        | 16GB  | Prototyping only     |
    | A100      | 40GB  | Both modes           |
    | A100 XL   | 80GB  | Production only      |
    | H100      | 80GB  | Production only      |

    ### CPU and RAM

    **Prototyping mode**: Configure vCPUs with 8GB RAM per vCPU.
    - 4 vCPUs = 32GB RAM
    - 8 vCPUs = 64GB RAM
    - 16 vCPUs = 128GB RAM
    - 32 vCPUs = 256GB RAM

    **Production mode**: Fixed at 18 vCPUs and 144GB RAM per GPU.
    - 2 GPUs = 36 vCPUs, 288GB RAM
    - 4 GPUs = 72 vCPUs, 576GB RAM

    <Note>
      By default, 4 vCPUs and 32GB of memory are included with prototyping instances. Additional vCPUs are billed hourly at the rates shown on the [pricing page](https://www.thundercompute.com/pricing).
    </Note>

    ### Templates

    Templates pre-configure your instance for common AI workflows:

    | Template       | Description                           |
    |----------------|---------------------------------------|
    | `base`         | Ubuntu with PyTorch + CUDA            |
    | `ollama`       | Ollama server environment             |
    | `comfy-ui`     | ComfyUI for AI image generation       |
    | `comfy-ui-wan` | ComfyUI with Wan2.1 pre-installed     |
    | `webui-forge`  | WebUI Forge for Stable Diffusion      |

    After connecting to an instance with a template, start the server with:

    ```bash
    start-ollama      # or start-comfy-ui, etc.
    ```

    See [Using Instance Templates](/guides/using-instance-templates) for more details.

    ### Restore from Snapshot

    Your snapshots appear alongside templates in the selection menu. Select a snapshot to create an instance with your previously saved state.

    ```bash
    tnr create
    # Select your snapshot in the "Templates" step
    ```

  </Tab>
  <Tab title="VSCode / Cursor / Windsurf">
    ### Create an Instance

    1. Open the Thunder Compute sidebar panel
    2. Click the **Create Instance** button (or the `+` icon)
    3. Select your configuration options in the dropdown menus

    {/* TODO: Add screenshot of create instance panel in VSCode */}

    ### Configuration Options

    The extension provides the same options as the CLI:

    - **Mode**: Prototyping or Production
    - **GPU Type**: T4, A100, A100 XL, H100 (availability depends on mode)
    - **vCPUs**: 4, 8, 16, or 32 (prototyping only)
    - **Disk Size**: 100GB to 400GB (prototyping) or 1000GB (production)
    - **Template**: Base, Ollama, ComfyUI, and more

    {/* TODO: Add screenshot of configuration options in extension */}

  </Tab>
  <Tab title="Console">
    ### Create an Instance

    1. Log in to the [console](https://console.thundercompute.com)
    2. Click **Create Instance**
    3. Select your mode, GPU, CPU, disk size, and template
    4. Click **Launch**

    {/* TODO: Add screenshot of console create instance page */}

    The console provides the same configuration options as the CLI and extension.

  </Tab>
</Tabs>

<Note>
  Instance storage is ephemeral. Back up important data externally before deleting an instance. See [Using Ephemeral Storage](/guides/using-ephemeral-storage) for recommended workflows.
</Note>
