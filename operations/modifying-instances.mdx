---
title: "Modifying Instances"
description: "Change the resources of a running Thunder Compute instance"
icon: "sliders"
iconType: "solid"
---

Modify the resources of a running instance without creating a new one. This allows you to scale up or down based on your workload needs.

<Tabs>
  <Tab title="CLI">
    ### Modify an Instance

    Use the modify command with the instance ID and new configuration:

    ```bash
    tnr modify <instance_id> [options]
    ```

    ### Available Options

    | Option       | Description                              |
    |--------------|------------------------------------------|
    | `--gpu`      | GPU type (e.g., `t4`, `a100`, `h100`)    |
    | `--vcpus`    | Number of virtual CPUs                   |
    | `--ram`      | Amount of RAM                            |
    | `--disk-size-gb` | Disk size (can only be increased)    |
    | `--mode`     | Switch between `prototyping` and `production` |

    ### Examples

    ```bash
    # Switch to a different GPU
    tnr modify 0 --gpu a100

    # Scale up CPUs and RAM
    tnr modify 0 --vcpus 16 --ram 128

    # Increase disk size
    tnr modify 0 --disk-size-gb 300

    # Switch to production mode
    tnr modify 0 --mode production
    ```

    <Note>
      Disk size can only be increased, not decreased.
    </Note>

  </Tab>
  <Tab title="VSCode / Cursor / Windsurf">
    ### Modify an Instance

    1. Open the Thunder Compute sidebar panel
    2. Right-click on a running instance
    3. Select **Modify Instance**
    4. Adjust the resources in the dialog

    {/* TODO: Add screenshot of modify option in context menu */}

    {/* TODO: Add screenshot of modify dialog */}

  </Tab>
  <Tab title="Console">
    ### Modify an Instance

    1. Find your running instance on the dashboard
    2. Click the **Modify** button (or the settings icon)
    3. Adjust the configuration
    4. Click **Save Changes**

    {/* TODO: Add screenshot of modify button in console */}

    {/* TODO: Add screenshot of modify dialog */}

  </Tab>
</Tabs>

## What Can Be Modified

| Resource    | Can Modify? | Notes                                           |
|-------------|-------------|-------------------------------------------------|
| GPU Type    | Yes         |                                                 |
| vCPUs       | Yes         |                                                 |
| RAM         | Yes         |                                                 |
| Mode        | Yes         | Switch between prototyping and production       |
| Disk Size   | Increase only | Cannot shrink disk                            |
| GPU Count   | No          | Requires new instance                           |
